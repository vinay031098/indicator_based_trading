# ─── Core ──────────────────────────────────────────────────────────
flask==3.0.3
werkzeug==3.0.3
gunicorn==22.0.0
requests==2.31.0
python-dotenv==1.0.1

# ─── Data / analysis ───────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4

# ─── Broker SDK ────────────────────────────────────────────────────
# NOTE: fyers-apiv3 declares the obsolete `asyncio` PyPI backport as a dependency,
# which shadows stdlib asyncio and breaks installs. Install it WITHOUT deps:
#     pip install --no-deps fyers-apiv3==3.1.7
# (handled in Dockerfile / render.yaml buildCommand). Its real runtime deps are
# Its real runtime deps (fyers-apiv3 is installed with --no-deps in Docker/render):
websocket-client==1.8.0
aiohttp==3.9.3
protobuf==5.29.3

# ─── Config / validation ───────────────────────────────────────────
pydantic==2.7.4
pydantic-settings==2.3.4

# ─── Security ──────────────────────────────────────────────────────
flask-limiter==3.7.0
flask-talisman==1.1.0
flask-wtf==1.2.1
cryptography==42.0.8

# ─── Persistence ───────────────────────────────────────────────────
SQLAlchemy==2.0.31
alembic==1.13.2
# psycopg2-binary is only needed when DATABASE_URL points at Postgres.
psycopg2-binary==2.9.9

# ─── Caching / background jobs ─────────────────────────────────────
Flask-Caching==2.3.0
redis==5.0.7
rq==1.16.2

# ─── Export ────────────────────────────────────────────────────────
openpyxl==3.1.5
